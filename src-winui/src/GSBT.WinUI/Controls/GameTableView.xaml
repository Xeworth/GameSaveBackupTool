<?xml version="1.0" encoding="UTF-8" ?>
<UserControl
    x:Class="GSBT.WinUI.Controls.GameTableView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:GSBT.WinUI.ViewModels"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="GridCellReadOnlyText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />
            <Setter Property="IsTextSelectionEnabled" Value="False" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>
        <!-- Table body: ThemeResource tracks live shell theme (code-built SolidColorBrush does not). -->
        <Style x:Key="GsbtTableCellBodyTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{ThemeResource GsbtBodyTextBrush}" />
            <Setter Property="IsTextSelectionEnabled" Value="False" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>
        <Style x:Key="GsbtTableHeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{ThemeResource GsbtBodyTextBrush}" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>
        <Style x:Key="GsbtTableRowCellBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="{ThemeResource GsbtTableGridLineBrush}" />
        </Style>
        <Style x:Key="GsbtTableHeaderCellBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="{ThemeResource GsbtTableGridLineBrush}" />
        </Style>
        <Style x:Key="GsbtTableRowRootStyle" TargetType="Grid">
            <Setter Property="BorderBrush" Value="{ThemeResource GsbtTableGridLineBrush}" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
        </Style>
        <Style x:Key="GsbtGameListViewItemStyle" BasedOn="{StaticResource DefaultListViewItemStyle}" TargetType="ListViewItem">
            <Setter Property="Padding" Value="0" />
            <Setter Property="MinHeight" Value="28" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="UseSystemFocusVisuals" Value="False" />
            <!-- Avoid Fluent item chrome lines that can disagree with our Gsbt grid during theme transitions. -->
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
    </UserControl.Resources>

    <Border
        Padding="1"
        Background="{ThemeResource GsbtCardBgBrush}"
        BorderBrush="{ThemeResource GsbtBorderBrush}"
        BorderThickness="1"
        CornerRadius="8">
        <Grid x:Name="RootGrid" Background="{ThemeResource GsbtCardBgBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border
                Grid.Row="0"
                Background="{ThemeResource GsbtTableHeaderBgBrush}"
                BorderBrush="{ThemeResource GsbtTableGridLineBrush}"
                BorderThickness="0,0,0,1"
                Padding="0,8,0,8">
                <Grid x:Name="HeaderGrid" />
            </Border>

            <Grid x:Name="ListHost" Grid.Row="1" Background="Transparent">
                <ListView
                    x:Name="GamesGrid"
                    Padding="0"
                    Background="Transparent"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    CanDragItems="False"
                    CanReorderItems="False"
                    ContainerContentChanging="GamesGrid_ContainerContentChanging"
                    IsItemClickEnabled="False"
                    IsSwipeEnabled="False"
                    ItemContainerStyle="{StaticResource GsbtGameListViewItemStyle}"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                    SelectionMode="Extended">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="vm:GameRowViewModel">
                            <Grid x:Name="PART_RowRoot" Height="28" Style="{StaticResource GsbtTableRowRootStyle}" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>
    </Border>
</UserControl>

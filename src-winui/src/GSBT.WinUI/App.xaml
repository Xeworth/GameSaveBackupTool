<?xml version="1.0" encoding="UTF-8" ?>
<Application
    x:Class="GSBT.WinUI.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:GSBT.WinUI.Controls"
    xmlns:local="using:GSBT.WinUI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <ResourceDictionary Source="Themes/GsbtPyPalette.xaml" />
                <ResourceDictionary Source="Themes/GsbtMenuFlyout.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <!-- All ComboBoxes: classic list drops below (footer MenuFlyouts stay separate). -->
            <Style TargetType="ComboBox" BasedOn="{StaticResource DefaultComboBoxStyle}">
                <Setter Property="controls:GsbtComboBoxChrome.ClassicDropdown" Value="True" />
            </Style>
            <Style TargetType="controls:GsbtComboBox" BasedOn="{StaticResource DefaultComboBoxStyle}" />
            <!-- Tray / Scan / Help menus: padded presenter + rounded item hover (see GsbtMenuFlyoutChrome). -->
            <Style
                x:Key="GsbtMenuFlyoutPresenterStyle"
                TargetType="MenuFlyoutPresenter"
                BasedOn="{StaticResource DefaultMenuFlyoutPresenterStyle}">
                <Setter Property="Padding" Value="3" />
                <Setter Property="CornerRadius" Value="8" />
            </Style>
            <Style
                x:Key="GsbtMenuFlyoutItemStyle"
                TargetType="MenuFlyoutItem"
                BasedOn="{StaticResource DefaultMenuFlyoutItemStyle}">
                <Setter Property="MinHeight" Value="32" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Padding" Value="6,3,6,3" />
                <Setter Property="CornerRadius" Value="4" />
                <Setter Property="Template" Value="{StaticResource GsbtMenuFlyoutItemTemplate}" />
            </Style>
            <Style TargetType="MenuFlyoutItem" BasedOn="{StaticResource GsbtMenuFlyoutItemStyle}" />
        </ResourceDictionary>
    </Application.Resources>
</Application>

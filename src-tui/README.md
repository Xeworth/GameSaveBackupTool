# GSBT Lite — Python Textual / TUI

Terminal UI edition. **Source will be added here** when imported from your maintained Textual or curses tree.

## Status

This folder is reserved in the monorepo. The old repository Python GUI was removed; this edition is not the same codebase.

## Run (after import)

Follow the README you add when copying the project (typically `pip install -r requirements.txt` and a `main.py` or module entry).

For the active Windows-native edition, use [../src-winui/README.md](../src-winui/README.md).
